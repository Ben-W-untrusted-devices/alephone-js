{
  "name": "alephone-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Browser-side glue code for the Aleph One web port (data provisioning, DOM/input bridging). Does not contain the engine itself, which is compiled to WASM from Source_Files.",
  "scripts": {
    "dev": "vite",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "jsdom": "^25.0.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
